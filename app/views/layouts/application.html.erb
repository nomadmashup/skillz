<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Skillz" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="<%= skills_path %>">Skillz</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><a href="<%= skills_path %>" title="<%= @current_user.email %>"><strong><%= "#{@current_user.first_name} #{@current_user.last_name}" %></strong></a></li>
              <hr class="hidden-desktop">
              <li><a href="#" title="Change to another person"><small>Change...</small></a></li>
              <li><a href="<%= csv_path %>" title="Export info to a CSV file"><small>Export</small></a></li>
              <li><a href="<%= about_path %>" title="About the Skillz project"><small>About</small></a></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">

      <div class="row-fluid">
        <div class="span12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Nomad Ninjas 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script type="text/javascript">
      var skillz_user = "<%= @current_user.email %>";
    </script>

    <%= javascript_include_tag "application" %>

  </body>
</html>
